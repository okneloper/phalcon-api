version: '3'

services:
  nginx:
    image: nginx:1.13
    ports:
    - 80:80
    volumes:
    - .:/usr/share/nginx/html
    - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
    - php

  php:
    image: mileschou/phalcon:7.2-fpm
    volumes:
    - .:/usr/share/nginx/html
